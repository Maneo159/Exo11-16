version: '3.8'

services:
  postgres:
    image: postgres:16-alpine
    container_name: exo11-postgres
    environment:
      POSTGRES_DB: exo11
      POSTGRES_USER: postgres
      POSTGRES_PASSWORD: postgres
    ports:
      - "5432:5432"
    volumes:
      - postgres_data:/var/lib/postgresql/data
    networks:
      - exo11-network
    healthcheck:
      test: ["CMD-SHELL", "pg_isready -U postgres"]
      interval: 10s
      timeout: 5s
      retries: 5

volumes:
  postgres_data:

networks:
  exo11-network:
    driver: bridge

